{
  "buildCommand": "cd ../.. && npm ci && cd apps/frontend && npx expo export -p web --output-dir dist",
  "outputDirectory": "dist",
  "installCommand": "cd ../.. && npm ci",
  "devCommand": "npx expo start --web",
  "framework": null,
  "functions": {},
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
